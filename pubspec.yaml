name: riverpod_example
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  adaptive_dialog:
  cupertino_icons:
  flutter:
    sdk: flutter
  flutter_hooks:
  gap:
  hooks_riverpod:
  recase:
  simple_logger:
  state_notifier:
  uuid:

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic_mono:

flutter:
  uses-material-design: true
